<%- model_class = Ubicacion -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:ubicacion_id) %></th>
      <th><%= model_class.human_attribute_name(:longitud) %></th>
      <th><%= model_class.human_attribute_name(:latitud) %></th>
      <th><%= model_class.human_attribute_name(:fecha) %></th>
      <th><%= model_class.human_attribute_name(:usuario_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @ubicaciones.each do |ubicacion| %>
      <tr>
        <td><%= link_to ubicacion.id, ubicacion_path(ubicacion) %></td>
        <td><%= ubicacion.ubicacion_id %></td>
        <td><%= ubicacion.longitud %></td>
        <td><%= ubicacion.latitud %></td>
        <td><%= ubicacion.fecha %></td>
        <td><%= ubicacion.usuario_id %></td>
        <td><%=l ubicacion.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_ubicacion_path(ubicacion), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      ubicacion_path(ubicacion),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_ubicacion_path,
            :class => 'btn btn-primary' %>
