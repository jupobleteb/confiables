<%- model_class = Empresa -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:rut) %></th>
      <th><%= model_class.human_attribute_name(:nombre) %></th>
      <th><%= model_class.human_attribute_name(:acronimo) %></th>
      <th><%= model_class.human_attribute_name(:rubro) %></th>
      <th><%= model_class.human_attribute_name(:rutrepresentante) %></th>
      <th><%= model_class.human_attribute_name(:email) %></th>
      <th><%= model_class.human_attribute_name(:telefono_id) %></th>
      <th><%= model_class.human_attribute_name(:direccion_id) %></th>
      <th><%= model_class.human_attribute_name(:usuario_id) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @empresas.each do |empresa| %>
      <tr>
        <td><%= link_to empresa.id, empresa_path(empresa) %></td>
        <td><%= empresa.rut %></td>
        <td><%= empresa.nombre %></td>
        <td><%= empresa.acronimo %></td>
        <td><%= empresa.rubro %></td>
        <td><%= empresa.rutrepresentante %></td>
        <td><%= empresa.email %></td>
        <td><%= empresa.telefono_id %></td>
        <td><%= empresa.direccion_id %></td>
        <td><%= empresa.usuario_id %></td>
        <td><%=l empresa.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_empresa_path(empresa), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      empresa_path(empresa),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_empresa_path,
            :class => 'btn btn-primary' %>
